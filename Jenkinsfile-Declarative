pipeline{
    agent any
    stages {
        stage('build') {
            steps {
            // One or more steps need to be included within the steps block.
                script{
                    echo "Build made successfully"
                }
            }
        }

        stage('test') {
            steps {
            // One or more steps need to be included within the steps block.
                script{
                    echo "Testing phase"
                }
            }
        }

    }

}